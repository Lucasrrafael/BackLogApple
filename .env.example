# Vercel Postgres — copiado automaticamente ao vincular o banco no dashboard
POSTGRES_URL=postgres://...

# Senha compartilhada entre os membros do time para criar/apagar/marcar tasks
# Gere com: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
BACKLOG_SECRET=troque-por-uma-senha-forte
